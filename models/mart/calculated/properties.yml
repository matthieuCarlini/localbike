version: 2

models:
  - name: mrt_calculated__customer_summary
    description: customer oriented mart. granularity is customer.
    columns:
      - name: total_orders
        description: "Number of orders by each customer"
      - name: total_amount
        description: "total amount by customer"
      - name: customer_type
        description: "Type of customer : one time or regular buyer"